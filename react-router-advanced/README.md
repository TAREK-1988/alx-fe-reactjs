# React Router Advanced

This project demonstrates:
- React Router setup in App.jsx
- Nested routes inside Profile (ProfileDetails & ProfileSettings)
- Dynamic routes for blog posts (/blog/:postId)
- Protected routes that require authentication to access Profile

## Run
```bash
npm install
npm run dev
